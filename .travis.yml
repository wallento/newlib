sudo: required

services:
  - docker

before_install:
  - docker pull ubuntu

script:
  - docker run ubuntu /bin/sh -c "cd /root/; apt-get install -y wget; wget https://raw.githubusercontent.com/wallento/newlib-travis/master/build.sh; bash build.sh"
