sudo: false

addons:
  apt:
    packages:
    - build-essential
    - libgmp-dev
    - libmpfr-dev
    - libmpc-dev
    - zlib1g-dev
    - texinfo
    - flex
    - bison

before_install:
  - git clone https://github.com/wallento/newlib-travis.git

script:
  - ./newlib-travis/build.sh
